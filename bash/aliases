# Bash aliases file.

alias ll="ls -lah --group-directories-first"
alias powerline-refresh="powerline-daemon -q -r"


# Include workstation specific aliases if present.
if [ -f ~/.aliases/custom ]; then
  . ~/.aliases/custom
fi
if [ -f ~/.aliases/work ]; then
  . ~/.aliases/work
fi
